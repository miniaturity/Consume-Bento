using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using LevelData = GameManager.LevelData;
public class LevelManager : MonoBehaviour
{
    public LevelData[] levels = {
        new LevelData {
            width = 4,
            height = 4,
            cells = {
                
            }
        }
    };
}
